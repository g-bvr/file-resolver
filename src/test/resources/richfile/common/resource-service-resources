#SETTINGS ../settings/resource-service
#IF ${MEMORY}
#ELSE
  #SET MEMORY ${RESOURCE_MEMORY_DEFAULT}
#END
#IF ${CPU}
#ELSE
  #SET CPU ${RESOURCE_CPU_DEFAULT}
#END
#IF ${STORAGE}
#ELSE
  #SET STORAGE ${RESOURCE_STORAGE_DEFAULT}
#END